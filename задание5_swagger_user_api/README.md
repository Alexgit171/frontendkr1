# Практическое занятие 5 — Расширенный REST API + Swagger

## Что описано в Swagger
- схема `User`;
- CRUD-операции:
  - `GET /api/users`
  - `POST /api/users`
  - `GET /api/users/{id}`
  - `PATCH /api/users/{id}`
  - `DELETE /api/users/{id}`
